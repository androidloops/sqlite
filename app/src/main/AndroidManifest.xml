<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobizone.notessqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.mobizone.notessqlite.ui.main.MainActivity"
            android:label="@string/choose"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.mobizone.notessqlite.ui.notes.nativeimplementation.NativeImplementationActivity"
            android:label="@string/native_implementation"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.mobizone.notessqlite.ui.notes.roomImplementation.RoomImplementationActivity"
            android:label="@string/room_implementation"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>